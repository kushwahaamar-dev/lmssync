# Canvas Outlook Sync - Production Dependencies
# Install with: pip install -r requirements.txt

# HTTP client for API calls
requests>=2.28.0

# Microsoft Authentication Library for OAuth 2.0
msal>=1.20.0

# URL handling (dependency of requests, pinned for security)
urllib3>=1.26.0
